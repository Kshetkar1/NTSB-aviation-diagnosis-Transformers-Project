# Structural Mapping + Retrieval

**Status:** EXPLORATORY — section 8/9 generators; not main eval path.

## Layout

- `worked_examples/, tests/` — code and runners
- `docs_StructMap/, presentations_StructMap/` — notes and reports
- `outputs/` — run artifacts

See repo root `PROJECT_MAP.md` for how this approach relates to `Frozen-BN-Narrative-Evidence-2026-07-20/` (current paper path).
