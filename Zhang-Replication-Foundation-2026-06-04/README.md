# Zhang Replication Foundation

**Status:** REFERENCE — vendored paper code, BTS xlsx, NTSB.xdsl, replication scripts.

## Layout

- `scripts/, reference/` — code and runners
- `docs_Zhang/` — notes and reports
- `outputs/` — run artifacts

See repo root `PROJECT_MAP.md` for how this approach relates to `Frozen-BN-Narrative-Evidence-2026-07-20/` (current paper path).
