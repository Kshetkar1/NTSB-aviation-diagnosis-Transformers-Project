# LLM Narrative Parser Experiments

**Status:** REJECTED for main paper (tier-2 fallback only in Frozen-BN).

## Layout

- `tests/` — code and runners
- `docs_LLM/` — notes and reports
- `outputs/` — run artifacts

See repo root `PROJECT_MAP.md` for how this approach relates to `Frozen-BN-Narrative-Evidence-2026-07-20/` (current paper path).
