# Embedding Similarity Counting Path

**Status:** SUPERSEDED by Frozen-BN for paper; kept for ablations and Zhang counting parity.

## Layout

- `tests/, code/` — code and runners
- `docs_Embedding/` — notes and reports
- `outputs/` — run artifacts

See repo root `PROJECT_MAP.md` for how this approach relates to `Frozen-BN-Narrative-Evidence-2026-07-20/` (current paper path).
